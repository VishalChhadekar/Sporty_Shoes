server:
  port: 9050

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://docker-mysql:3306/sporty_shoes
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  file:
    name: application.log
